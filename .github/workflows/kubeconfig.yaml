name: anotther

on:
  pull_request:
  push:
    branches: [main]
    tags: [v*]
  workflow_dispatch:
    inputs:
      deploy:
        description: Set to `true` to deploy to K8s
        required: false


jobs:
  kubeconfig:
    runs-on: ubuntu-latest
    steps:
      - run: |
          export test='marwen'
      - run: env

